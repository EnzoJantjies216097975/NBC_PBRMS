import { Placeholder } from '@/components/Placeholder';

export default function TransportListPage() {
  return (
    <Placeholder title="Transport List">
      Compile the pickup list (only crew who need transport) with address, phone, pickup &amp;
      drop-off times — printable for the Transport Office. Coming next phase.
    </Placeholder>
  );
}
